<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BankBalanceCheckApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/marcellus_regular</item>
        <item name="android:statusBarColor">@color/appBar</item>
    </style>

    <style name="Theme.BankBalanceCheckApp" parent="Base.Theme.BankBalanceCheckApp" />
</resources>